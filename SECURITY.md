# Security

- Do not commit `.env`, API keys, private documents, generated indexes, or uploaded files.
- The bundled web application is intended for a trusted local environment and has no authentication.
- Before public deployment, add authentication, HTTPS, request limits, malware scanning, and tenant isolation.
- Report sensitive vulnerabilities privately to the repository owner instead of opening a public issue.
